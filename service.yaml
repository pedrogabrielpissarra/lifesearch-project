apiVersion: v1
kind: Service
metadata:
  name: lifesearch
  labels:
    app: lifesearch
spec:
  selector:
    app: lifesearch
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
